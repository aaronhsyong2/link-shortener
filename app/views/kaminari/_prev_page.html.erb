<% if current_page.first? %>
  <span class="page-link opacity-30 cursor-default"><%= t('views.pagination.previous').html_safe %></span>
<% else %>
  <%= link_to t('views.pagination.previous').html_safe, url, rel: 'prev', remote: remote, class: "page-link" %>
<% end %>
