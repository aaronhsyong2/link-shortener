<% if current_page.last? %>
  <span class="page-link opacity-30 cursor-default"><%= t('views.pagination.last').html_safe %></span>
<% else %>
  <%= link_to t('views.pagination.last').html_safe, url, remote: remote, class: "page-link" %>
<% end %>
