<% if current_page.first? %>
  <span class="page-link opacity-30 cursor-default"><%= t('views.pagination.first').html_safe %></span>
<% else %>
  <%= link_to t('views.pagination.first').html_safe, url, remote: remote, class: "page-link" %>
<% end %>
