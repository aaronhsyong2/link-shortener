<% if current_page.last? %>
  <span class="page-link opacity-30 cursor-default"><%= t('views.pagination.next').html_safe %></span>
<% else %>
  <%= link_to t('views.pagination.next').html_safe, url, rel: 'next', remote: remote, class: "page-link" %>
<% end %>
