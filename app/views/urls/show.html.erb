<% content_for :title, @url.title || "URL Details" %>

<div class="max-w-2xl mx-auto">
  <h1 class="text-3xl font-bold text-gray-900 mb-6">URL Details</h1>

  <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
    <div class="p-6 space-y-5">
      <div>
        <label class="block text-xs font-medium text-gray-500 uppercase tracking-wide mb-1">Short URL</label>
        <div class="flex items-center space-x-3">
          <code class="text-lg font-mono text-blue-600 break-all">
            <%= short_redirect_url(short_code: @url.short_code) %>
          </code>
          <button
            data-controller="clipboard"
            data-clipboard-short-url-value="<%= short_redirect_url(short_code: @url.short_code) %>"
            data-action="click->clipboard#copy"
            class="shrink-0 rounded-md bg-gray-100 hover:bg-gray-200 px-3 py-1.5 text-xs font-medium text-gray-700 transition cursor-pointer"
          >
            Copy
          </button>
        </div>
      </div>

      <div>
        <label class="block text-xs font-medium text-gray-500 uppercase tracking-wide mb-1">Target URL</label>
        <p class="text-sm text-gray-900 break-all">
          <%= link_to @url.target_url, safe_url(@url.target_url), target: "_blank", class: "text-blue-600 hover:underline" %>
        </p>
      </div>

      <div class="flex space-x-8">
        <div>
          <label class="block text-xs font-medium text-gray-500 uppercase tracking-wide mb-1">Title</label>
          <p class="text-sm text-gray-900"><%= @url.title || "No title available" %></p>
        </div>
        <div>
          <label class="block text-xs font-medium text-gray-500 uppercase tracking-wide mb-1">Clicks</label>
          <p class="text-sm text-gray-900"><%= @url.clicks_count %></p>
        </div>
      </div>
    </div>

    <div class="bg-gray-50 border-t border-gray-200 px-6 py-4 flex space-x-3">
      <%= link_to "View Report", url_report_path(@url),
        class: "rounded-lg px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium transition" %>
      <%= link_to "Shorten Another", root_path,
        class: "rounded-lg px-4 py-2 bg-white border border-gray-300 hover:bg-gray-50 text-gray-700 text-sm font-medium transition" %>
      <%= link_to "All URLs", urls_path,
        class: "rounded-lg px-4 py-2 bg-white border border-gray-300 hover:bg-gray-50 text-gray-700 text-sm font-medium transition" %>
    </div>
  </div>
</div>
